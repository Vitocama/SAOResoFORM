<Window x:Class="SAOResoForm.AttestatiControl.AttestazioniInserimentoControl.AttestatiInserimentoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="INSERIMENTO DEGLI ATTESTATI" Height="600" Width="1400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TITOLO -->
        <TextBlock Grid.Row="0" 
                   Text="VISUALIZZA DATABASE" 
                   FontSize="20" 
                   FontWeight="Bold"
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Center"/>

        <!-- RICERCA E CONTATORE -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Text="Cerca Personale:" 
                       VerticalAlignment="Center" 
                       FontWeight="Bold" 
                       Margin="0,0,10,0"/>

            <TextBox Grid.Column="1"
                     Text="{Binding FiltroRicerca, UpdateSourceTrigger=PropertyChanged}"
                     VerticalAlignment="Center"
                     Height="30"
                     Padding="5"/>

            <!-- CONTATORE ELEMENTI -->
            <TextBlock Grid.Column="3"
                       HorizontalAlignment="Right"
                       VerticalAlignment="Center"
                       FontWeight="Bold"
                       Foreground="#2C3E50"
                       Margin="10,0,0,0">
                <Run Text="Elementi visualizzati: "/>
                <Run Text="{Binding FilteredPersonaleList.Count, Mode=OneWay}"/>
                <Run Text=" / "/>
                <Run Text="{Binding TotalePersonale, Mode=OneWay}"/>
            </TextBlock>
        </Grid>

        <!-- DATAGRID -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding FilteredPersonaleList}"
                  SelectedItem="{Binding PersonaleSelezionato}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  MouseDoubleClick="DataGrid_MouseDoubleClick"
                  Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="*"/>
                <DataGridTextColumn Header="Cognome" Binding="{Binding Cognome}" Width="*"/>
                <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>
                <DataGridTextColumn Header="Matricola" Binding="{Binding Matricola}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>