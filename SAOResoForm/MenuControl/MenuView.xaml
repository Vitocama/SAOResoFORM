<UserControl x:Class="SAOResoForm.MenuControl.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Width="155"
             Height="560"
             Margin="8,0,0,0">
    <Border BorderBrush="Black" BorderThickness="2" CornerRadius="5" Height="550">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="40"/>
                <RowDefinition Height="40"/>
                <RowDefinition Height="40"/>
                <RowDefinition Height="40"/>
                <RowDefinition Height="40"/>
                <RowDefinition Height="40"/>
            </Grid.RowDefinitions>
            <!-- HOME -->
            <Border Grid.Row="0" Style="{StaticResource BorderMenu}">
                <Button Content="HOME" Style="{StaticResource MenuButton}" Command="{Binding OpenHomeCommand}"/>
            </Border>
            <!-- PERSONALE -->
            <Border Grid.Row="1" Style="{StaticResource BorderMenu}">
                <Button Content="PERSONALE" Style="{StaticResource MenuButton}" Command="{Binding OpenPersonaleCommand}"/>
            </Border>
            <!-- ATTESTATI -->
            <Border Grid.Row="2" Style="{StaticResource BorderMenu}">
                <Button Content="ATTESTATI" Style="{StaticResource MenuButton}" Command="{Binding OpenAttestatiCommand}"/>
            </Border>
            <!-- REPORTISTICHE -->
            <Border Grid.Row="3" Style="{StaticResource BorderMenu}">
                <Button Content="REPORTISTICHE" Style="{StaticResource MenuButton}" Command="{Binding OpenReportisticaCommand}"/>
            </Border>
            <!-- CREA ACCOUNT -->
            <Border Grid.Row="4" Style="{StaticResource BorderMenu}">
                <Button Content="CREA ACCOUNT" Style="{StaticResource MenuButton}"/>
            </Border>
            <!-- SCELTA DB -->
            <Border Grid.Row="5" Style="{StaticResource BorderMenu}">
                <Button Content="SCELTA DB" Style="{StaticResource MenuButton}" Command="{Binding openSceltaDBCommand}"/>
            </Border>
            <!-- LOGOUT -->
            <Border Grid.Row="6" Style="{StaticResource BorderMenu}">
                <Button Content="LOGOUT" Style="{StaticResource MenuButton}" Command="{Binding logout}"/>
            </Border>
        </Grid>
    </Border>
</UserControl>