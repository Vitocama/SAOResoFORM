<Window x:Class="SAOResoForm.DBScelta.SceltaDBView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SAOResoForm.DBScelta"
        mc:Ignorable="d"
        Title="Scelta Database" Height="300" Width="500">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Percorso Database:" FontWeight="Bold" Margin="0,0,0,5"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding PercorsoDb, UpdateSourceTrigger=PropertyChanged}" 
                     IsReadOnly="True" Padding="5" Margin="0,0,5,0"/>
            <Button Grid.Column="1" Content="Sfoglia..." Command="{Binding SfogliaCommand}" 
                    Width="80" Padding="5"/>
        </Grid>

        <Button Grid.Row="2" Content="Salva" Command="{Binding SalvaCommand}" 
                Width="100" HorizontalAlignment="Right" Margin="0,10,0,0" Padding="5"/>
    </Grid>
</Window>